<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Hardened-runtime entitlements for the FluidCAD shell. Used both for the
    outer .app (`mac.entitlements`) and for every nested helper, framework and
    binary (`mac.entitlementsInherit`) — see electron-builder.yml.

    Electron needs the first two to run V8's JIT under the hardened runtime.
    The engine (a Node child forked from the shell, plus native `.node`
    addons and wasm loaded from `resources/engine`) is not signed with the
    same team-scoped restrictions Apple's library validation expects, so
    validation is disabled; the child also inherits env vars such as
    NODE_OPTIONS/ELECTRON_RUN_AS_NODE, which needs the dyld allowance.
  -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>
</dict>
</plist>
