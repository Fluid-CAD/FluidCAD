{
  "name": "fluidcad-desktop",
  "version": "0.0.41",
  "private": true,
  "description": "The FluidCAD desktop shell: a window, native menus, and an engine manager.",
  "author": "Marwan Aouida <contact@marwan.dev>",
  "license": "MIT",
  "homepage": "https://fluidcad.io",
  "desktopName": "fluidcad-desktop.desktop",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "stage:engine": "node scripts/stage-engine.js",
    "dev": "npm run build && electron .",
    "pack": "npm run build && npm run stage:engine && electron-builder --dir",
    "dist": "npm run build && npm run stage:engine && electron-builder"
  },
  "dependencies": {
    "electron-updater": "^6.6.2",
    "tar": "^7.5.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.1",
    "electron": "^40.0.0",
    "electron-builder": "^26.0.12",
    "typescript": "^5.9.2"
  }
}
