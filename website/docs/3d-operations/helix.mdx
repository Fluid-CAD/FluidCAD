---
sidebar_position: 12
title: "Helix"
---

import {ExplainedCode} from '@site/src/components/docs/ExplainedCode';
import {ModeTable} from '@site/src/components/docs/ModeTable';
import {UiFigure} from '@site/src/components/docs/UiFigure';
import helixBasic from '!!raw-loader!./_examples/helix-basic.js';
import helixSpring from '!!raw-loader!./_examples/helix-spring.js';
import helixFace from '!!raw-loader!./_examples/helix-face.js';
import helixThread from '!!raw-loader!./_examples/helix-thread.js';
import helixGroove from '!!raw-loader!./_examples/helix-groove.js';
import helixTapered from '!!raw-loader!./_examples/helix-tapered.js';

# Helix

Helix creates a helical wire — the curve that springs, threads and coils are built from. On its own it renders as a wire; [sweep](/docs/3d-operations/sweep) a profile along it to make material.

## In the viewport

1. Click **Helix** on the toolbar.
2. Fill in the dialog. The wire previews as you type.
3. Click **Apply**. The wire appears in the viewport and the timeline; a sweep can then pick it as its path.

<UiFigure
  src="/img/docs/ui/helix-dialog.png"
  alt="The Helix dialog with the world Z axis, a radius, a pitch and a turn count"
  markers={[
    {n: 1, x: 5, y: 14, label: 'From axis / From face', description: 'Where the coil gets its frame. From axis takes an axis you click — a world axis in the viewport, an axis() feature, or a straight edge. From face reads the axis, radius and height off a cylindrical or conical face instead.'},
    {n: 2, x: 5, y: 26, label: 'Axis', description: 'The line the helix winds around. It opens on world Z; click another axis or a straight edge to change it.'},
    {n: 3, x: 5, y: 39, label: 'Radius / End radius', description: 'The radius of the coil, and — if you fill in End radius — the radius it tapers to, which makes a conical helix. Both are ignored when the source is a face or a circular edge, where the geometry sets them.'},
    {n: 4, x: 5, y: 55, label: 'Coil by', description: 'Turns or Pitch: which of the two you type. Turns is the number of revolutions, Pitch the axial rise per revolution.'},
    {n: 5, x: 5, y: 70, label: 'Height', description: 'Total axial extent. Any two of pitch, turns and height determine the third, so leaving this on auto lets the other two set it.'},
    {n: 6, x: 5, y: 82, label: 'Start / end offset', description: 'Run the ends past the source along the axis, in axial units — how a real thread runs off the end of its stock instead of stopping flush.'},
    {n: 7, x: 5, y: 93, label: 'Apply', description: 'Writes the statement and adds the timeline row; Exit writes nothing.'},
  ]}
/>

## The wire, and what it is for

On its own a helix is just a curve. It becomes material when a [sweep](/docs/3d-operations/sweep) carries a profile along it:

<ModeTable columns={[
  {title: 'The helix', code: 'helix("z")', image: '/img/docs/3d-operations/helix-basic.png', description: 'A wire coiling around the Z axis. Nothing solid — no faces, no volume.'},
  {title: 'Swept', code: 'sweep(h, profile)', image: '/img/docs/3d-operations/helix-spring.png', description: 'A small circle swept along that wire: a spring. The profile is placed at the path start automatically.'},
]} />

<ExplainedCode code={helixBasic} fileName="coil.part.js" title="The helix" />

The argument is the axis to coil around — a named axis like `"z"`, anything axis-like, or an existing scene object (see [deriving from geometry](#deriving-from-geometry)).

## Sizing

Three chained methods control the proportions, and any two determine the third:

```js
helix("z").pitch(10).turns(4)       // height = 40
helix("z").pitch(10).height(40)     // turns = 4
helix("z").turns(4).height(40)      // pitch = 10
```

| Method | Meaning | Default |
|--------|---------|---------|
| `.pitch(p)` | Axial rise per full revolution | `height / turns` |
| `.turns(n)` | Number of revolutions; fractions are fine | 1 |
| `.height(h)` | Total axial extent | 50, or `pitch × turns` |
| `.radius(r)` | Radius of the coil | 20 |

Winding is clockwise (left-handed) by default; `.ccw()` gives a right-handed helix.

## Springs

The spring above is the whole recipe: a helix for the path, a small circle for the wire section, and a sweep to join them.

<ExplainedCode code={helixSpring} fileName="spring.part.js" />

## Deriving from geometry

With **From face** in the dialog — or a scene object as the argument in code — the helix takes its frame from the geometry:

- **Cylindrical face** — axis, radius and height come from the face.
- **Conical face** — the helix follows the cone's taper; radii are derived from the face (`.radius()` / `.endRadius()` are ignored).
- **Line edge** — the line becomes the axis, its length the height.
- **Circular edge** — the circle's normal becomes the axis, its radius the helix radius.

![Helix from a cylindrical face](/img/docs/3d-operations/helix-face.png)

<ExplainedCode code={helixFace} />

Use `select(...)` to narrow a body down to the single face or edge the helix should read from.

## Threads and grooves

A helix at the surface of a cylinder is the spine of a thread. Sweep a profile along it and the result fuses with the cylinder (the Add tab):

![Helical thread](/img/docs/3d-operations/helix-thread.png)

<ExplainedCode code={helixThread} />

The sweep's **Remove** tab carves a groove instead:

![Helical groove](/img/docs/3d-operations/helix-groove.png)

<ExplainedCode code={helixGroove} />

### Running past the ends

Real threads run off the ends of the stock rather than stopping flush. **start offset** and **end offset** shift the helix ends along the axis, in axial units:

```js
helix("z").height(50).startOffset(-5).endOffset(5)   // 5 units overshoot each side
```

A negative start offset extends the start; a positive end offset extends the end (and vice versa to trim). On a conical face the extension follows the cone's natural taper.

## Tapered helixes

When **end radius** differs from **Radius**, the radius blends linearly along the height — a conical helix:

![Tapered helix spring](/img/docs/3d-operations/helix-tapered.png)

<ExplainedCode code={helixTapered} />

The same shape falls out automatically when the source is a conical face. `.endRadius()` is ignored for face and circular-edge sources, where the radii come from the geometry.

## Limitations

- Turns must be positive and the pitch non-zero; a tapered helix must keep its end radius above zero.
- A face source must be cylindrical or conical; an edge source must be a line or a circle. Multi-shape sources need a `select(...)` to single out one face or edge.
