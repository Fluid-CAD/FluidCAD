---
sidebar_position: 5
title: "Sweep"
---

import {ExplainedCode} from '@site/src/components/docs/ExplainedCode';
import {ModeTable} from '@site/src/components/docs/ModeTable';
import {UiFigure} from '@site/src/components/docs/UiFigure';
import sweepBefore from '!!raw-loader!./_examples/sweep-before.js';
import sweepBasic from '!!raw-loader!./_examples/sweep-basic.js';
import sweepExtend from '!!raw-loader!./_examples/sweep-extend.js';
import sweepThin from '!!raw-loader!./_examples/sweep-thin.js';
import sweepThinOpen from '!!raw-loader!./_examples/sweep-thin-open.js';
import sweepScopeAdd from '!!raw-loader!./_examples/sweep-scope-add.js';
import sweepThinAdd from '!!raw-loader!./_examples/sweep-thin-add.js';

# Sweep

Sweep moves a profile along a path to make a solid: pipes, rails, handles, cable runs — anything that follows a curve. It takes two sketches, a profile and a path, and they should lie on different planes.

## In the viewport

1. Draw the path as a sketch (an open chain of lines and arcs, or a [helix](/docs/3d-operations/helix)) and the profile as a second sketch on a plane that crosses the path, usually at its start. Then click **Sweep** on the toolbar.
2. Fill in the dialog — both slots are usually filled for you from the two most recent sketches.
3. Click **Apply**.

<UiFigure
  src="/img/docs/ui/sweep-dialog.png"
  alt="The Sweep dialog with a profile sketch and a path sketch picked"
  markers={[
    {n: 1, x: 5, y: 16, label: 'Add / Remove / New', description: 'What the swept solid does to the model — fuse, cut, or stand alone. See Add, New and Remove below.'},
    {n: 2, x: 5, y: 33, label: 'Sketch', description: 'The profile that travels along the path. One sketch; click a sketch row in the timeline or its wires in the viewport to change it.'},
    {n: 3, x: 5, y: 50, label: 'Path', description: 'The curve to follow — a sketch, a helix, or edges picked in the viewport. Edge picking stays live while the dialog is open: clicking an edge re-sources the path to edges, clicking a sketch switches it back.'},
    {n: 4, x: 5, y: 60, label: 'Thin walls', description: 'Sweeps a wall of the thickness you type instead of the filled profile — a pipe rather than a rod.'},
    {n: 5, x: 5, y: 78, label: 'Scope', description: 'Which solids the Add or Remove applies to.'},
    {n: 6, x: 5, y: 92, label: 'Apply', description: 'Writes the statement and adds the timeline row; Exit writes nothing.'},
  ]}
/>

## From two sketches to a pipe

A ring profile and a bent path, and the solid they make between them:

<ModeTable columns={[
  {title: 'Before', code: 'path + profile', image: '/img/docs/3d-operations/sweep-before.png', description: 'Seen square to the front plane: the path — a line and two arcs, tangent where they join — with the ring profile edge-on at its start, the short line at the bottom.'},
  {title: 'After', code: 'sweep(spine, profile)', image: '/img/docs/3d-operations/sweep-basic.png', description: 'The ring carried along the path, staying square to it the whole way. The hole in the profile becomes the bore.'},
]} />

<ExplainedCode code={sweepBefore} fileName="pipe.part.js" title="The two sketches" />

<ExplainedCode code={sweepBasic} title="The complete file" />

The first argument of `sweep()` is the path (spine), the second the profile.

## Extend

`.extend()` continues the swept solid past the end of the path, straight along the path's tangent there. Use it for lead-ins and run-outs — a pipe that runs straight past its last bend:

![Sweep extended past the path end](/img/docs/3d-operations/sweep-extend.png)

<ExplainedCode code={sweepExtend} />

```js
sweep(spine, profile).extend("end", 80)                      // 80 units past the end
sweep(spine, profile).extend("start", 20)                    // 20 units before the start
sweep(spine, profile).extend("start", 20).extend("end", 80)  // both ends
```

## Add, New and Remove

The pictures use a drawer front and a Ø16 circle swept along an arch drawn on its front face. The circle is centred on the face, so the tube runs half in, half out of the panel. Only the tab changes:

<ModeTable columns={[
  {title: 'Add', code: 'sweep(path, profile)', image: '/img/docs/3d-operations/sweep-scope-add.png', description: 'The tube fuses with the panel — a rounded pull rail.'},
  {title: 'New', code: 'sweep(path, profile).new()', image: '/img/docs/3d-operations/sweep-scope-new.png', description: 'The tube is a separate solid — a rail seated in the panel.'},
  {title: 'Remove', code: 'sweep(path, profile).remove()', image: '/img/docs/3d-operations/sweep-scope-remove.png', description: 'The tube\'s volume is cut out — a rounded finger-pull channel.'},
]} />

<ExplainedCode code={sweepScopeAdd} title="The Add column's code">
The profile sits on `plane(path, 'start')`: a plane at the start of the path, normal to it, so the circle is swept square to the curve. The New and Remove columns only change the highlighted line.
</ExplainedCode>

## Thin walls

Turn on **Thin** for a pipe instead of a rod: the profile's edges are offset by the thickness and the band between them is swept.

![Thin sweep](/img/docs/3d-operations/sweep-thin.png)

<ExplainedCode code={sweepThin} />

```js
sweep(spine, profile).thin(5)     // 5 units outward
sweep(spine, profile).thin(-5)    // 5 units inward
sweep(spine, profile).thin(5, -3) // dual offset
```

### Thin in the three modes

The same rail with **Thin** on and a wall of 2:

<ModeTable columns={[
  {title: 'Add', code: 'sweep(path, profile).thin(2)', image: '/img/docs/3d-operations/sweep-thin-add.png', description: 'A 2-thick tube wall fused with the panel.'},
  {title: 'New', code: 'sweep(path, profile).thin(2).new()', image: '/img/docs/3d-operations/sweep-thin-new.png', description: 'The tube wall as a separate body.'},
  {title: 'Remove', code: 'sweep(path, profile).thin(2).remove()', image: '/img/docs/3d-operations/sweep-thin-remove.png', description: 'Only the wall is cut — two curved slots; the core between them stays.'},
]} />

<ExplainedCode code={sweepThinAdd} title="The Add column's code" />

### Open profiles

Thin sweep also works with open profiles. The ends are capped — select them with `capFaces()` / `capEdges()`:

![Thin sweep — open profile](/img/docs/3d-operations/sweep-thin-open.png)

<ExplainedCode code={sweepThinOpen} />

## Accessing geometry

```js
const s = sweep(spine, profile)

s.endFaces()       // face at the end of the path
s.startFaces()     // face at the start of the path
s.sideFaces()      // the swept surface(s)
s.endEdges()       // edges at the end
s.startEdges()     // edges at the start
s.sideEdges()      // edges along the sides
s.internalFaces()  // internal faces (if profile has holes)
s.internalEdges()  // internal edges
s.capFaces()       // cap face(s) of a thin sweep (open profile)
s.capEdges()       // cap edges of a thin sweep (open profile)
```

## Fusion scope

```js
sweep(spine, profile).new()                // create a separate solid
sweep(spine, profile).add()                // fuse with touching solids (default)
sweep(spine, profile).remove()             // subtract from all intersecting solids
sweep(spine, profile).remove().scope(box)  // subtract only from the box
```
