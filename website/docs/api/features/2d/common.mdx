---
sidebar_position: 32
title: "common"
description: "Computes the common (intersection) of all shapes in the current context."
---

# common()

Computes the common (intersection) of all shapes in the current context.

**Returns**: [**`Common`**](/docs/api/types/common)

:::tip
See the [guide](/docs/booleans/introduction) for detailed usage examples.
:::

## Signatures

```ts
common(): Common
```

Computes the common (intersection) of all shapes in the current context.

---

```ts
common(...objects: SceneObject[]): Common
```

Computes the common (intersection) of the given shapes.

| Parameter | Type | Description |
|-----------|------|-------------|
| `...objects` | [SceneObject](/docs/api/types/scene-object)[] | The objects to intersect *(optional)* |
