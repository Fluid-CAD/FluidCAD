---
sidebar_position: 21
title: "connect"
description: "Connects the current position back to the start to close the sketch."
---

# connect()

Connects the current position back to the start to close the sketch.

**Returns**: [**`Geometry`**](/docs/api/types/geometry)

## Signatures

```ts
connect(mode?: ConnectMode): Geometry
```

Connects the current position back to the start to close the sketch.

| Parameter | Type | Description |
|-----------|------|-------------|
| `mode` | ConnectMode | The connection mode *(optional)* |
