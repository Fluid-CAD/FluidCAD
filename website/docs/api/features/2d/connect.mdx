---
sidebar_position: 22
title: "connect"
description: "Closes the current polyline: draws a bridge from the current position back to the polyline's start (the last absolutely-positioned statement — an absolute move() or an explicit-start segment)."
---

# connect()

Closes the current polyline: draws a bridge from the current position
back to the polyline's start (the last absolutely-positioned statement —
an absolute move() or an explicit-start segment).

**Returns**: [**`Geometry`**](/docs/api/types/geometry)

## Signatures

```ts
connect(mode?: ConnectMode): Geometry
```

Closes the current polyline: draws a bridge from the current position
back to the polyline's start (the last absolutely-positioned statement —
an absolute move() or an explicit-start segment).

| Parameter | Type | Description |
|-----------|------|-------------|
| `mode` | ConnectMode | The connection mode *(optional)* |
