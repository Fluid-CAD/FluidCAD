---
sidebar_position: 4
title: "ellipse"
description: "Draws an ellipse at a given center. The center is a solver point — `.center()` references it in constraints, so the solve positions the ellipse; the radii stay fixed literals."
---

# ellipse()

Draws an ellipse at a given center. The center is a solver point —
`.center()` references it in constraints, so the solve positions the
ellipse; the radii stay fixed literals.

**Returns**: [**`Ellipse`**](/docs/api/types/ellipse)

:::tip
See the [guide](/docs/guides/sketching/primitives) for detailed usage examples.
:::

## Signatures

```ts
ellipse(center: Point2DLike, rx: NumberParam, ry: NumberParam): Ellipse
```

Draws an ellipse at a given center. The center is a solver point —
`.center()` references it in constraints, so the solve positions the
ellipse; the radii stay fixed literals.

| Parameter | Type | Description |
|-----------|------|-------------|
| `center` | [Point2DLike](/docs/api/types/point2dlike) | The center point |
| `rx` | NumberParam | Semi-radius along the plane's X axis |
| `ry` | NumberParam | Semi-radius along the plane's Y axis |
