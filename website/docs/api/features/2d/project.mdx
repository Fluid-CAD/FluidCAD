---
sidebar_position: 25
title: "project"
description: "Projects 3D objects — faces, edges, selections — or previous sketches onto the current sketch plane. The output registers as FIXED reference geometry — constraints may target it (`tangent(p, l)`, `p.ref(i)`, `p.center()`)."
---

# project()

Projects 3D objects — faces, edges, selections — or previous sketches
onto the current sketch plane. The output registers as FIXED reference
geometry — constraints may target it (`tangent(p, l)`, `p.ref(i)`,
`p.center()`).

A sketch source (`project(s1)`, or an entity it returned such as
`project(s1.geometries.c)`) is referenced, never consumed: its geometry
is read as its own body left it, so the reference works whether or not
an extrude has already used the sketch, and the sketch stays available
to later features. A sketch cannot project its own geometry.

**Returns**: [**`Reference`**](/docs/api/types/reference)

:::tip
See the [guide](/docs/sketching/tools/project) for detailed usage examples.
:::

## Signatures

```ts
project(...sourceObjects: SceneObject[]): Reference
```

Projects 3D objects — faces, edges, selections — or previous sketches
onto the current sketch plane. The output registers as FIXED reference
geometry — constraints may target it (`tangent(p, l)`, `p.ref(i)`,
`p.center()`).

A sketch source (`project(s1)`, or an entity it returned such as
`project(s1.geometries.c)`) is referenced, never consumed: its geometry
is read as its own body left it, so the reference works whether or not
an extrude has already used the sketch, and the sketch stays available
to later features. A sketch cannot project its own geometry.

| Parameter | Type | Description |
|-----------|------|-------------|
| `...sourceObjects` | [SceneObject](/docs/api/types/scene-object)[] | The 3D objects or sketches to project *(optional)* |
