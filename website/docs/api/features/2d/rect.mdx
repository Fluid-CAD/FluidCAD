---
sidebar_position: 5
title: "rect"
description: "Draws a rectangle with the given width and optional height."
---

# rect()

Draws a rectangle with the given width and optional height.

**Returns**: [**`Rect`**](/docs/api/types/rect)

:::tip
See the [guide](/docs/guides/sketching/primitive-shapes) for detailed usage examples.
:::

## Signatures

```ts
rect(width: NumberParam, height?: NumberParam): Rect
```

Draws a rectangle with the given width and optional height.

| Parameter | Type | Description |
|-----------|------|-------------|
| `width` | NumberParam | The rectangle width |
| `height` | NumberParam | The rectangle height (defaults to width) *(optional)* |

---

```ts
rect(start: Point2DLike, width: NumberParam, height?: NumberParam): Rect
```

Draws a rectangle at a given start point.

| Parameter | Type | Description |
|-----------|------|-------------|
| `start` | [Point2DLike](/docs/api/types/point2dlike) | The start point (bottom-left corner) |
| `width` | NumberParam | The rectangle width |
| `height` | NumberParam | The rectangle height (defaults to width) *(optional)* |

---

```ts
rect(targetPlane: SceneObject | PlaneLike, width: NumberParam, height: NumberParam): Rect
```

Draws a rectangle with given dimensions on a specific plane.

| Parameter | Type | Description |
|-----------|------|-------------|
| `targetPlane` | [SceneObject](/docs/api/types/scene-object) \| [PlaneLike](/docs/api/types/plane-like) | The plane to draw on |
| `width` | NumberParam | The rectangle width |
| `height` | NumberParam | The rectangle height |
