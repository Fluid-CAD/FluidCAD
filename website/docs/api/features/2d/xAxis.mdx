---
sidebar_position: 11
title: "xAxis"
description: "The sketch x axis — the infinite line through the origin along the plane's x direction — as a constraint target. A fixed reference line: `collinear(xAxis(), l)`, `coincident(p, xAxis())`, `symmetric(a, b, xAxis())`, `distance(xAxis(), c, 25)`. Constraint sketches only."
---

# xAxis()

The sketch x axis — the infinite line through the origin along the
plane's x direction — as a constraint target. A fixed reference line:
`collinear(xAxis(), l)`, `coincident(p, xAxis())`,
`symmetric(a, b, xAxis())`, `distance(xAxis(), c, 25)`.
Constraint sketches only.

**Returns**: **`SketchDatum`**

## Signatures

```ts
xAxis(): SketchDatum
```

The sketch x axis — the infinite line through the origin along the
plane's x direction — as a constraint target. A fixed reference line:
`collinear(xAxis(), l)`, `coincident(p, xAxis())`,
`symmetric(a, b, xAxis())`, `distance(xAxis(), c, 25)`.
Constraint sketches only.
