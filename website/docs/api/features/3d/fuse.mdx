---
sidebar_position: 9
title: "fuse"
description: "Fuses all shapes in the current context."
---

# fuse()

Fuses all shapes in the current context.

**Returns**: [**`SceneObject`**](/docs/api/types/scene-object)

:::tip
See the [guide](/docs/guides/booleans-and-fusion) for detailed usage examples.
:::

## Signatures

```ts
fuse(): Fuse
```

Fuses all shapes in the current context.

---

```ts
fuse(...objects: SceneObject[]): Fuse
```

Fuses the given shapes into one.

| Parameter | Type | Description |
|-----------|------|-------------|
| `...objects` | [SceneObject](/docs/api/types/scene-object)[] | The objects to fuse together *(optional)* |
