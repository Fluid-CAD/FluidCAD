---
sidebar_position: 4
title: "load"
description: "Loads an imported model (the cached STEP import in `imports/`) by name."
---

# load()

Loads an imported model (the cached STEP import in `imports/`) by name.

Imports are cached in mm and scaled into this document's unit
automatically, so a unit is normally unnecessary. `options.unit` asserts
the unit the asset's cached geometry is in, for assets without
trustworthy metadata; it overrides the import sidecar.

**Returns**: [**`SceneObject`**](/docs/api/types/scene-object)

:::tip
See the [guide](/docs/import-export/import) for detailed usage examples.
:::

## Signatures

```ts
load(fileName: string, options?: LoadOptions): ILoadFile
```

Loads an imported model (the cached STEP import in `imports/`) by name.

Imports are cached in mm and scaled into this document's unit
automatically, so a unit is normally unnecessary. `options.unit` asserts
the unit the asset's cached geometry is in, for assets without
trustworthy metadata; it overrides the import sidecar.

| Parameter | Type | Description |
|-----------|------|-------------|
| `fileName` | string | The asset name (the STEP file name without extension) |
| `options` | LoadOptions | Optional `{ unit }` assertion of the asset's unit *(optional)* |
