---
sidebar_position: 4
title: "load"
description: "Loads a 3D model file (STEP, STL, etc.) by filename."
---

# load()

Loads a 3D model file (STEP, STL, etc.) by filename.

**Returns**: [**`SceneObject`**](/docs/api/types/scene-object)

:::tip
See the [guide](/docs/guides/import) for detailed usage examples.
:::

## Signatures

```ts
load(fileName: string): SceneObject
```

Loads a 3D model file (STEP, STL, etc.) by filename.

| Parameter | Type | Description |
|-----------|------|-------------|
| `fileName` | string | The path to the model file |
