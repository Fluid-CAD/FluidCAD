---
sidebar_position: 2
title: "Fuse"
---

import {ExplainedCode} from '@site/src/components/docs/ExplainedCode';
import {ModeTable} from '@site/src/components/docs/ModeTable';
import {UiFigure} from '@site/src/components/docs/UiFigure';
import booleanFuse from '!!raw-loader!./_examples/boolean-fuse.js';

# Fuse

Fuse joins two or more solids into one. Everything the solids cover between them stays; the walls where they overlap disappear. Use it when bodies were kept apart on purpose — with the **New** tab, or because they came in from a STEP file — and now have to behave as one piece.

## In the viewport

1. Click **Boolean** on the toolbar and stay on the **Fuse** tab.
2. Click each solid you want to join. Clicking any face or edge picks the whole solid, and each pick lands in the **Solids** slot as a numbered chip.
3. Click **Apply**.

<UiFigure
  src="/img/docs/ui/boolean-fuse-dialog.png"
  alt="The Boolean dialog on the Fuse tab with two solids picked"
  markers={[
    {n: 1, x: 5, y: 25, label: 'Fuse tab', description: 'Chooses the operation. Switching tabs keeps the solids you have already picked, so you can try one and change your mind.'},
    {n: 2, x: 5, y: 38, label: 'Solids', description: 'Takes as many solids as you like — two is the common case. The blue border means this slot is armed, so the next pick lands here.'},
    {n: 3, x: 5, y: 51, label: 'The chips', description: 'One per solid, numbered in pick order — that is the argument order in the statement. The grey number is the line the solid was made on; the arrow jumps there, the ✕ drops the pick, and clicking a solid that is already listed drops it too.'},
    {n: 4, x: 5, y: 88, label: 'Apply', description: 'Writes fuse(…) and replaces the picked solids with the result. Exit closes the dialog and writes nothing.'},
  ]}
/>

Two cubes that share a corner, before and after:

<ModeTable columns={[
  {title: 'Before', code: 'two solids', image: '/img/docs/booleans/boolean-separate.png', description: 'Two cubes sharing a corner. Each one still draws its own edges through the other, and the Shapes panel lists two rows.'},
  {title: 'After', code: 'fuse(a, b)', image: '/img/docs/booleans/boolean-fuse.png', description: 'One solid, one outline. The faces inside the shared corner are gone and the Shapes panel lists a single row.'},
]} />

<ExplainedCode code={booleanFuse} fileName="cubes.part.js">
The two cubes go in and one staircase-shaped body comes out. Nothing else about the model changes — a fuse only removes the walls where the solids overlapped.
</ExplainedCode>

## Code forms

```js
import { fuse } from 'fluidcad/core';

fuse(a, b)          // join two solids
fuse(a, b, c)       // any number
fuse()              // every solid in the current context
```

The result is a single solid that later features treat as one body. Its new geometry — the faces and edges that only exist because the solids met — is reachable through the feature's accessors:

```js
const f = fuse(a, b)
f.internalEdges()    // edges created where the inputs met
f.internalFaces()
```

## Fuse or just let them touch?

You rarely need `fuse()` for material you are creating right now: the **Add** tab already merges a new extrusion, revolve or sweep into whatever it touches. That is [auto-fusion](/docs/3d-operations/introduction#add-new-and-remove), and it covers most modelling.

`fuse()` is for solids that already exist side by side. If two of your bodies should never merge — because they are different components — a [`part()`](/docs/part/part-definition) is the better tool: shapes fuse inside a part and never across parts.
