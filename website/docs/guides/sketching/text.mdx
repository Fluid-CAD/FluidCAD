---
sidebar_position: 5
title: "Text"
---

import CodeBlock from '@theme/CodeBlock';
import compoundText from '!!raw-loader!./_examples/compound-text.js';
import compoundTextFonts from '!!raw-loader!./_examples/compound-text-fonts.js';
import compoundTextExtruded from '!!raw-loader!./_examples/compound-text-extruded.js';
import compoundTextPath from '!!raw-loader!./_examples/compound-text-path.js';
import compoundTextPathAlignStart from '!!raw-loader!./_examples/compound-text-path-align-start.js';
import compoundTextPathFlip from '!!raw-loader!./_examples/compound-text-path-flip.js';
import compoundTextPathOffset from '!!raw-loader!./_examples/compound-text-path-offset.js';

# Text

`text()` turns a string into outline geometry — each letter becomes a closed profile you can extrude into 3D lettering. Use it inside a `sketch()` with `text("Hello")`. The size is the cap height in model units; thickness comes from `extrude()`.

<CodeBlock language="js">{compoundText}</CodeBlock>

![Text outline](/img/docs/sketching/compound-text.png)

:::info[Placing text]
The anchored form `text("Hello")` draws at the sketch plane's origin. Move it with the `.at([x, y])` modifier — `text("Hello").at([0, 20])` — or lay the text along a `.guide()` line or curve (`text("Hello", path)`), which also gives you full control over direction.
:::

## Fonts and weight

By default `text()` uses a system font. Pass a **system font name** to `.font()`, or a **workspace-relative font file** ending in `.ttf`/`.otf` (e.g. `.font("fonts/Brand.ttf")`). Control the typeface with `.weight()` (`"regular"`, `"bold"`, or a number 100–900) and `.italic()`:

<CodeBlock language="js">{compoundTextFonts}</CodeBlock>

![Bold serif text from a named system font](/img/docs/sketching/compound-text-fonts.png)

If the named font isn't installed, FluidCAD substitutes the closest match (e.g. `"Arial"` → a system sans-serif) and ultimately falls back to a default font.

## Extruding text

Because `text()` is ordinary sketch geometry, you extrude it like any other profile — the counters (the holes in `o`, `e`, `A`, …) are kept automatically:

<CodeBlock language="js">{compoundTextExtruded}</CodeBlock>

![Extruded 3D text](/img/docs/sketching/compound-text-extruded.png)

For multi-line strings (`\n`), set `.align("left" | "center" | "right")`, and fine-tune spacing with `.lineSpacing()` and `.letterSpacing()`.

## Text along a path

`text(string, path)` lays the glyphs along a curve instead of a straight baseline. The path can be a curve in the same sketch, a whole sketch (e.g. `sketch("xy", () => circle([0, 0], 80))`), or edges selected from existing geometry (e.g. `select(edge().circle())` for a cylinder rim). The path must be planar; the text lies in the path's plane.

<CodeBlock language="js">{compoundTextPath}</CodeBlock>

![Text along an arc](/img/docs/sketching/compound-text-path.png)

The path itself stays in the scene — several texts can share one path. When the path lives in the same sketch, mark it `.guide()` so the curve stays out of the extruded profile; text follows guide curves just fine.

Glyph spacing is measured as arc length, so kerning and `.letterSpacing()` behave exactly like straight text. To slide the text start along the path by an arc-length distance, use `.startAt(d)`.

### Aligning on a path

`.align()` positions the run against the path (`"left"` and `"right"` work as synonyms of `"start"` and `"end"`):

<CodeBlock language="js">{compoundTextPathAlignStart.replace(/^\/\/ @screenshot.*\n/, '')}</CodeBlock>

`"start"` (the default) begins the text at the path's start:

![Path text aligned to the start](/img/docs/sketching/compound-text-path-align-start.png)

`"center"` centers it on the path's midpoint:

![Path text centered on the path](/img/docs/sketching/compound-text-path-align-center.png)

`"end"` finishes at the path's end:

![Path text aligned to the end](/img/docs/sketching/compound-text-path-align-end.png)

`"space-between"` justifies the glyphs evenly across the whole path, first glyph at the start and last at the end:

![Path text justified with space-between](/img/docs/sketching/compound-text-path-align-space-between.png)

`"space-around"` also distributes the glyphs evenly, but with half a gap before the first glyph and after the last — like the CSS flexbox value:

![Path text distributed with space-around](/img/docs/sketching/compound-text-path-align-space-around.png)

### Outside or inside: flip

On a closed path (a full circle) the text wraps around and sits on the **outside** by default — `"space-between"` and `"space-around"` then space the glyphs evenly around the loop. `.flip()` mirrors the text to the other side of the path and reverses the reading direction: on a circle it moves the text inside; on an open curve it mirrors the text below the path:

<CodeBlock language="js">{compoundTextPathFlip}</CodeBlock>

![Text outside and inside a circle](/img/docs/sketching/compound-text-path-flip.png)

### Floating off the path: offset

`.offset(d)` shifts the baseline perpendicular to the path — positive values float the text above the curve, negative values tuck it below:

<CodeBlock language="js">{compoundTextPathOffset}</CodeBlock>

![Text offset above and below an arc](/img/docs/sketching/compound-text-path-offset.png)
