---
sidebar_position: 10
title: "Equal"
---

import {ExplainedCode} from '@site/src/components/docs/ExplainedCode';
import {ModeTable} from '@site/src/components/docs/ModeTable';
import {UiFigure} from '@site/src/components/docs/UiFigure';
import constraintEqual from '!!raw-loader!../../_examples/constraint-equal.js';

# Equal

`equal` gives two or more lines the same length, or two or more circles/arcs the same radius. One dimension on the first entity then sizes all of them.

## In the viewport

1. Click two or more lines, or two or more circles / arcs (all one kind, all distinct).
2. Click **Equal**. Every entity gets an `=` badge.

<UiFigure
  src="/img/docs/ui/constraint-bar-equal.png"
  alt="The constraint bar with two circles picked and the Equal button highlighted"
  rings={[{x: 39.2, y: 11.6, w: 6.9, h: 44.2}]}
  caption="Two circles picked: Equal is enabled. Two or more lines enable it too."
/>

<ModeTable columns={[
  {title: 'Before', image: '/img/docs/sketching/constraint-equal-before.png', description: 'Two holes drawn at two different sizes: ⌀20 and ⌀12.'},
  {title: 'After', code: 'equal(h1, h2)', image: '/img/docs/sketching/constraint-equal.png', description: 'The second hole takes the first one\'s size, so one diameter sizes both.'},
]} />

<ExplainedCode code={constraintEqual} fileName="holes.part.js">
`equal(h1, h2)` equates the second hole to the first (1 DOF), so the single `diameter(h1, 20)` sizes both. With more entities — `equal(c1, c2, c3, c4)` on four corner rounds — every entity after the first follows it, and one radius sizes all four.
</ExplainedCode>

## In code

```js
equal(l1, l2)                    // same length: 1 DOF
equal(c1, c2)                    // same radius: 1 DOF
equal(a, b, c, ...)              // every entity after the first: 1 DOF each
```

Lines and round entities cannot be mixed in one statement, and a datum axis has no length to equate.

## See also

- [Distance](/docs/sketching/constraints/dimensional/distance), [Diameter](/docs/sketching/constraints/dimensional/diameter) — the one dimension that drives an equal group
- [Symmetric](/docs/sketching/constraints/positional/symmetric)
