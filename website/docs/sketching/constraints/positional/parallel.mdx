---
sidebar_position: 5
title: "Parallel"
---

import {ExplainedCode} from '@site/src/components/docs/ExplainedCode';
import {ModeTable} from '@site/src/components/docs/ModeTable';
import {UiFigure} from '@site/src/components/docs/UiFigure';
import constraintParallel from '!!raw-loader!../../_examples/constraint-parallel.js';

# Parallel

`parallel` gives two or more lines the same direction. The first line is the reference; every other line turns to match it, and turns again whenever the reference turns.

## In the viewport

1. Click two or more distinct lines. The first click is the reference.
2. Click **Parallel**. Each following line gets a `∥` badge.

<UiFigure
  src="/img/docs/ui/constraint-bar-parallel.png"
  alt="The constraint bar with two lines picked and the Parallel button highlighted"
  rings={[{x: 20.6, y: 11.6, w: 6.9, h: 44.2}]}
  caption="Two lines picked: Parallel is enabled. The first pick is the reference."
/>

<ModeTable columns={[
  {title: 'Before', image: '/img/docs/sketching/constraint-parallel-before.png', description: 'The two flanks lean at different angles.'},
  {title: 'After', code: 'parallel(leftFlank, rightFlank)', image: '/img/docs/sketching/constraint-parallel.png', description: 'The right flank turns to match the left one\'s lean.'},
]} />

<ExplainedCode code={constraintParallel} fileName="channel.part.js">
The left flank's lean comes from `angle(xAxis(), leftFlank, 110)`; `parallel(leftFlank, rightFlank)` removes 1 DOF from the right flank so it follows. Change the angle and the channel leans as one, keeping its width.
</ExplainedCode>

## In code

```js
parallel(a, b)                   // b parallel to a: 1 DOF
parallel(a, b, c, ...)           // every line after the first: 1 DOF each
```

A datum axis can be the reference: `parallel(xAxis(), l)` is equivalent to `horizontal(l)`.

## See also

- [Perpendicular](/docs/sketching/constraints/positional/perpendicular)
- [Distance](/docs/sketching/constraints/dimensional/distance) — the gap between two parallel lines is a line–line distance
