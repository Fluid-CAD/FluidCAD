---
sidebar_position: 6
title: "Perpendicular"
---

import {ExplainedCode} from '@site/src/components/docs/ExplainedCode';
import {ModeTable} from '@site/src/components/docs/ModeTable';
import {UiFigure} from '@site/src/components/docs/UiFigure';
import constraintPerpendicular from '!!raw-loader!../../_examples/constraint-perpendicular.js';

# Perpendicular

`perpendicular` holds two lines at 90° to each other. Unlike `horizontal` + `vertical`, it says nothing about the sketch axes — tilt one line and the other follows, still square.

## In the viewport

1. Click two lines.
2. Click **Perpendicular**. The pair gets a `⊥` badge at their junction (or on the second line when they do not meet).

<UiFigure
  src="/img/docs/ui/constraint-bar-perpendicular.png"
  alt="The constraint bar with two lines picked and the Perpendicular button highlighted"
  rings={[{x: 26.8, y: 11.6, w: 6.9, h: 44.2}]}
  caption="Two lines picked: Perpendicular is enabled."
/>

<ModeTable columns={[
  {title: 'Before', image: '/img/docs/sketching/constraint-perpendicular-before.png', description: 'The bracket\'s corners are drawn roughly; none of them is square.'},
  {title: 'After', code: 'perpendicular(foot, upright) …', image: '/img/docs/sketching/constraint-perpendicular.png', description: 'Every corner square to the foot and the upright, on the 20° incline.'},
]} />

<ExplainedCode code={constraintPerpendicular} fileName="bracket.part.js">
The foot is held at 20° by an `angle` against the X axis. Five `perpendicular` statements (1 DOF each) square every corner of the bracket to the foot and the upright rather than to the sketch axes; two line–line `distance` dimensions set the leg thickness. Change the incline and the whole bracket rotates, still square.
</ExplainedCode>

## In code

```js
perpendicular(a, b)              // 1 DOF
```

Both arguments are lines; the segments do not need to touch. For a fixed 90° that should *also* follow the sketch axes, use `horizontal` and `vertical` instead — they are cheaper for the solver and read better in the timeline.

## See also

- [Parallel](/docs/sketching/constraints/positional/parallel)
- [Angle](/docs/sketching/constraints/dimensional/angle) — any angle other than 90°
